<h1>Experience</h1>
<% experience.list.map((current, i) => { %>
  <section id="experience_<%= i %>" class="education joined-section">
    <div class="column education__meta">
      <span class="education__meta__name"><%= current.name %></span>
      <span class="education__meta__dates">
        <% current.dates.map((date, di) => { %>
          <%- di === 0 ? "" : current.datedelimiter || " - " %>
          <%- ["NOW_YEAR"].includes(date) ? new Date().getFullYear() : date %>
        <% }) %>
      </span>
      <span class="education__meta__qualification"><%= current.role %></span>
    </div>
    <div class="column education__description">
      <p>
        <%= current.description %>
      </p>
    </div>
  </section>
<% }) %>